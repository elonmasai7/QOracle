apiVersion: apps/v1
kind: Deployment
metadata:
  name: quantumrisk-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: quantumrisk-redis
  template:
    metadata:
      labels:
        app: quantumrisk-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: quantumrisk-redis
spec:
  selector:
    app: quantumrisk-redis
  ports:
    - port: 6379
      targetPort: 6379
